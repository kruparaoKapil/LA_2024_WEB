import { CharacterwithspecialDirective } from './characterwithspecial.directive';

describe('CharacterwithspecialDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterwithspecialDirective();
    expect(directive).toBeTruthy();
  });
});
